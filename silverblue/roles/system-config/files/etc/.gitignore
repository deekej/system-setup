# begin section managed by etckeeper (do not edit this section by hand)

# new and old versions of conffiles, stored by apt/rpm
*.rpmnew
*.rpmorig
*.rpmsave

# old versions of files
*.old

# mount(8) records system state here, no need to store these
blkid.tab
blkid.tab.old

# some other files in /etc that typically do not need to be tracked
nologin
ld.so.cache
prelink.cache
mtab
mtab.fuselock
.pwd.lock
*.LOCK
network/run
adjtime
lvm/cache
lvm/archive
X11/xdm/authdir/authfiles/*
ntp.conf.dhcp
.initctl
webmin/fsdump/*.status
webmin/webmin/oscache
apparmor.d/cache/*
service/*/supervise/*
service/*/log/supervise/*
sv/*/supervise/*
sv/*/log/supervise/*
*.elc
*.pyc
*.pyo
init.d/.depend.*
openvpn/openvpn-status.log
cups/subscriptions.conf
cups/subscriptions.conf.O
fake-hwclock.data
check_mk/logwatch.state

# editor temp files
*~
.*.sw?
.sw?
\#*\#
DEADJOE

# end section managed by etckeeper

# Add .etckeeper containging files permissions info:
! .etckeeper

# Add files from /etc/alternatives
! alternatives/*

# Some of the files in profile.d are managed by a separate sub-repository:
profile.d/.git*
profile.d/default-bash-prompt.sh
profile.d/default-config.sh
profile.d/fortune-cookie.sh
profile.d/git-prompt.sh
profile.d/global-aliases.sh
profile.d/global-environment.sh

# Auxiliary files for system setup:
.setup/*

# E-mail aliases database:
aliases.db

# SSH keys created by some unknown process:
ssh/ssh_host_*

# Regularly changing / autogenerated files:
.updated
ntp.conf
resolv.conf
brlapi.key
pki/ca-trust/extracted/java/cacerts

# Cups settings:
printcap
cups/*

# Network configs constantly changes depending on the network we are connected to:
sysconfig/network-scripts/*.bak
sysconfig/network-scripts/ifcfg-*
sysconfig/network-scripts/keys-*
NetworkManager/system-connections/*

# Ignore active profile of tuned:
modprobe.d/tuned.conf
tuned/active_profile
tuned/profile_mode

# Ignore systemd-oomd related changes:
systemd/system.control/*

# Ignore configuration files of Virtual Machine Manager:
libvirt/*
